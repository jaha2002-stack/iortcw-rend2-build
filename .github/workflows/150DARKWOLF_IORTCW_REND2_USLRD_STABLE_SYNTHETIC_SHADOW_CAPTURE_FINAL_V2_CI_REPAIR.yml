name: Build DarkWolf Rend2 USLRD Stable Synthetic Shadow Capture Final v2 CI Repair over Run 35350987466 SP

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: darkwolf-rend2-uslrd-stable-synthetic-shadow-capture-final-v2-ci-repair-sp
  cancel-in-progress: false

jobs:
  build-windows-x64:
    name: USLRD Stable Synthetic Shadow Capture Final v2 CI Repair
    runs-on: ubuntu-24.04
    timeout-minutes: 120

    env:
      UPSTREAM_REPO: https://github.com/iortcw/iortcw.git
      UPSTREAM_SHA: 438e7d413b5f7277187c35b032eb0ef9093ae778

      PARENT_RUN_ID: "35350987466"
      PARENT_RUN_HEAD_SHA: 1cb4cfdc2248aa554b42cf606c858c56d53c25ed
      PARENT_WORKFLOW_PATH: '.github/workflows/148DARKWOLF_IORTCW_REND2_USLRD_ALWAYS_ON_DIRECT_SHADOW_CAPTURE_FIX_V1.yml'
      PARENT_WORKFLOW_BLOB_SHA: 9db3547f87f8748fc3dc1ddecfd518c4754d50bf
      PARENT_ARTIFACT_ID: "10549833909"
      PARENT_ARTIFACT_NAME: iortcw-rend2-uslrd-always-on-direct-shadow-capture-fix-v1-full-sp-windows-x64-1
      PARENT_ARTIFACT_DIGEST: sha256:082e48502e27b41f60c8dea5d4ac8e38e83f9f5605233c2a15f94fc374b1ced8
      PARENT_INNER_SHA256: 1327b8f5db538d2277c0d62e5dff1ddbbeb26dea1edbc9651f9af441a1a6e046
      PARENT_RELEASE_BASENAME: iortcw-rend2-uslrd-always-on-direct-shadow-capture-fix-v1-full-sp-windows-x64-1
      PARENT_CUMULATIVE_NAME: darkwolf-rend2-cumulative-uslrd-always-on-direct-shadow-capture-fix-v1-sp.patch
      PARENT_CUMULATIVE_SHA256: c96b6d1261431e183e904547f1ba95a933068e9693a8d601c2866242f2a4d377
      PARENT_CONTRACT_NAME: USLRD_ALWAYS_ON_DIRECT_SHADOW_CAPTURE_FIX_V1_CONTRACT.txt
      PARENT_CONTRACT_SHA256: 89028ffa370eba7a4d5be84f67cffc294646421b0642460b7d3b0ca8e3cc8599
      PARENT_RENDERER_SHA256: 6395cbf7d1358900a363cec7891bc80495e13c99221537870067e155eed16dd4

      PATCHER_NAME: apply_uslrd_stable_synthetic_shadow_capture_final_v2.py
      PATCHER_SHA256: 1b7794e9af289d0fd31317aa255bc30f8002106c62d6e46ad8b3217e554c9be7
      PATCHER_GZIP_SHA256: 97a2e2e141137166bb4ba8bb81f1397d69c58fea7d2ba13420c1bc2233a72351
      DELTA_NAME: darkwolf-uslrd-stable-synthetic-shadow-capture-final-v2-over-run-35350987466.patch
      CUMULATIVE_NAME: darkwolf-rend2-cumulative-uslrd-stable-synthetic-shadow-capture-final-v2-sp.patch
      CONTRACT_NAME: USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2_CONTRACT.txt
      VALIDATION_NAME: STATIC_VALIDATION_USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2.txt
      README_NAME: README_USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2_RU.txt
      TEST_PLAN_NAME: USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2_TEST_PLAN_RU.txt
      RELEASE_BASENAME: iortcw-rend2-uslrd-stable-synthetic-shadow-capture-final-v2-ci-repair-full-sp-windows-x64-1

    steps:
      - name: Checkout build repository
        uses: actions/checkout@v5

      - name: Install canonical MinGW and validation dependencies
        shell: bash
        run: |
          set -Eeuo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            binutils-mingw-w64-x86-64 build-essential ca-certificates \
            g++-mingw-w64-x86-64 gcc-mingw-w64-x86-64 gh git \
            libegl1 libegl-mesa0 libgl1 libgl1-mesa-dri make mingw-w64 jq \
            python3 python3-yaml unzip zip file

      - name: Preflight workflow YAML LF Bash Python and embedded patcher identity
        shell: bash
        run: |
          set -Eeuo pipefail
          workflow_rel="${GITHUB_WORKFLOW_REF#${GITHUB_REPOSITORY}/}"
          workflow_rel="${workflow_rel%@*}"
          test -f "$workflow_rel" || { echo "ERROR: workflow cannot be resolved: $workflow_rel" >&2; exit 1; }
          python3 - "$workflow_rel" <<'PY_PREFLIGHT'
          from pathlib import Path
          import base64,gzip,hashlib,re,subprocess,sys,yaml
          p=Path(sys.argv[1]); raw=p.read_bytes(); txt=raw.decode('utf-8')
          if b'\r' in raw: raise SystemExit('ERROR: CR byte in workflow; LF-only required')
          doc=yaml.safe_load(txt)
          expected='Build DarkWolf Rend2 USLRD Stable Synthetic Shadow Capture Final v2 CI Repair over Run 35350987466 SP'
          if doc.get('name') != expected: raise SystemExit(f'ERROR: workflow identity drift: {doc.get("name")!r}')
          checked=0; py_checked=0
          for job_name,job in (doc.get('jobs') or {}).items():
              for index,step in enumerate(job.get('steps') or [],1):
                  script=step.get('run')
                  if not script: continue
                  proc=subprocess.run(['bash','-n'],input=script,text=True,stdout=subprocess.PIPE,stderr=subprocess.PIPE)
                  if proc.returncode:
                      print(f'ERROR: bash -n failed job={job_name} step={index} name={step.get("name")}',file=sys.stderr)
                      print(proc.stderr,file=sys.stderr); raise SystemExit(proc.returncode)
                  checked += 1
                  lines=script.splitlines(); pos=0
                  while pos < len(lines):
                      hm=re.search(r"\bpython3\b.*<<'([^']+)'",lines[pos])
                      if not hm: pos += 1; continue
                      tag=hm.group(1); body=[]; pos += 1
                      while pos < len(lines) and lines[pos].strip()!=tag:
                          body.append(lines[pos]); pos += 1
                      if pos>=len(lines): raise SystemExit(f'ERROR: unclosed Python heredoc {tag}')
                      compile('\n'.join(body)+'\n',f'<workflow:{job_name}:{index}:{tag}>','exec')
                      py_checked += 1; pos += 1
          if checked < 11: raise SystemExit(f'ERROR: unexpectedly small Bash contract: {checked}')
          if py_checked < 4: raise SystemExit(f'ERROR: unexpectedly small Python contract: {py_checked}')
          start_marker='cat > "$PATCHER_NAME.gz.b64" <<\'PATCHER_B64\''
          end_marker='          PATCHER_B64'
          start=txt.find(start_marker)
          if start<0: raise SystemExit('ERROR: embedded patcher start marker missing')
          start=txt.find('\n',start)+1; end=txt.find('\n'+end_marker,start)
          if start<=0 or end<0: raise SystemExit('ERROR: embedded patcher end marker missing')
          enc=''.join(line.strip() for line in txt[start:end].splitlines())
          gz=base64.b64decode(enc,validate=True); py=gzip.decompress(gz)
          if hashlib.sha256(gz).hexdigest()!='97a2e2e141137166bb4ba8bb81f1397d69c58fea7d2ba13420c1bc2233a72351': raise SystemExit('ERROR: patcher gzip SHA drift')
          if hashlib.sha256(py).hexdigest()!='1b7794e9af289d0fd31317aa255bc30f8002106c62d6e46ad8b3217e554c9be7': raise SystemExit('ERROR: patcher SHA drift')
          if b'DARKWOLF_USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2' not in py: raise SystemExit('ERROR: patcher marker missing')
          print(f'Workflow preflight PASS: {checked} Bash blocks, {py_checked} Python blocks')
          PY_PREFLIGHT

      - name: Verify exact parent Run 35350987466
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -Eeuo pipefail
          run_json="$(gh api "/repos/${GITHUB_REPOSITORY}/actions/runs/${PARENT_RUN_ID}")"
          test "$(jq -r '.head_sha' <<<"$run_json")" = "$PARENT_RUN_HEAD_SHA" || { echo "ERROR: parent HEAD drift" >&2; exit 1; }
          test "$(jq -r '.conclusion' <<<"$run_json")" = success || { echo "ERROR: parent run not successful" >&2; exit 1; }
          test "$(jq -r '.path' <<<"$run_json")" = "$PARENT_WORKFLOW_PATH" || { echo "ERROR: parent workflow path drift" >&2; exit 1; }
          workflow_blob="$(gh api "/repos/${GITHUB_REPOSITORY}/contents/${PARENT_WORKFLOW_PATH}?ref=${PARENT_RUN_HEAD_SHA}" --jq '.sha')"
          test "$workflow_blob" = "$PARENT_WORKFLOW_BLOB_SHA" || { echo "ERROR: parent workflow blob drift: $workflow_blob" >&2; exit 1; }
          row="$(gh api "/repos/${GITHUB_REPOSITORY}/actions/runs/${PARENT_RUN_ID}/artifacts?per_page=100" --jq ".artifacts[] | select(.id == ${PARENT_ARTIFACT_ID}) | [.name,.expired,.digest] | @tsv")"
          test -n "$row" || { echo "ERROR: exact parent artifact missing" >&2; exit 1; }
          IFS=$'\t' read -r aname expired adigest <<< "$row"
          test "$aname" = "$PARENT_ARTIFACT_NAME" || { echo "ERROR: artifact name drift" >&2; exit 1; }
          test "$expired" = false || { echo "ERROR: artifact expired" >&2; exit 1; }
          test "$adigest" = "$PARENT_ARTIFACT_DIGEST" || { echo "ERROR: artifact digest drift: $adigest" >&2; exit 1; }
          echo "Exact parent Run 35350987466 provenance PASS"

      - name: Download and verify exact Run 35350987466 package
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -Eeuo pipefail
          rm -rf parent-artifact parent-release
          mkdir -p parent-artifact parent-release
          gh run download "$PARENT_RUN_ID" --repo "$GITHUB_REPOSITORY" --name "$PARENT_ARTIFACT_NAME" --dir parent-artifact
          z="parent-artifact/${PARENT_RELEASE_BASENAME}.zip"; sidecar="${z}.sha256"
          test -s "$z" && test -s "$sidecar"
          inner_sha="$(sha256sum "$z" | awk '{print $1}')"; sidecar_sha="$(awk '{print $1}' "$sidecar")"
          test "$inner_sha" = "$sidecar_sha" || { echo "ERROR: parent ZIP/sidecar mismatch" >&2; exit 1; }
          test "$inner_sha" = "$PARENT_INNER_SHA256" || { echo "ERROR: parent inner SHA drift: $inner_sha" >&2; exit 1; }
          unzip -tq "$z"; unzip -q "$z" -d parent-release
          mapfile -t roots < <(find parent-release -mindepth 1 -maxdepth 1 -type d -print | LC_ALL=C sort)
          test "${#roots[@]}" -eq 1 || { echo "ERROR: expected one parent release root" >&2; exit 1; }
          parent="${roots[0]}"
          test "$(basename "$parent")" = "$PARENT_RELEASE_BASENAME"
          printf '%s\n' "$parent" > .parent_stage_path
          (cd "$parent" && sha256sum -c docs/SHA256SUMS.txt)
          test "$(sha256sum "$parent/docs/$PARENT_CUMULATIVE_NAME" | awk '{print $1}')" = "$PARENT_CUMULATIVE_SHA256" || { echo "ERROR: parent cumulative SHA drift" >&2; exit 1; }
          test "$(sha256sum "$parent/docs/$PARENT_CONTRACT_NAME" | awk '{print $1}')" = "$PARENT_CONTRACT_SHA256" || { echo "ERROR: parent contract SHA drift" >&2; exit 1; }
          test "$(sha256sum "$parent/renderer_sp_rend2_x64.dll" | awk '{print $1}')" = "$PARENT_RENDERER_SHA256" || { echo "ERROR: parent renderer SHA drift" >&2; exit 1; }
          grep -Fq 'status=FIX_CANDIDATE_RUNTIME_ACCEPTANCE_REQUIRED' "$parent/docs/$PARENT_CONTRACT_NAME"
          grep -Fq 'attempt_budget=FINAL_ATTEMPT_1_OF_2' "$parent/docs/$PARENT_CONTRACT_NAME"
          grep -Fq 'staticpromote_max_active=32' "$parent/docs/$PARENT_CONTRACT_NAME"
          grep -Fq 'persistent_shadow_budget_default=UNCHANGED_3' "$parent/docs/$PARENT_CONTRACT_NAME"
          grep -Fq 'shadow_capture_separate_origin=SYNTHETIC_ONLY_OFFSET_TOWARD_CAMERA_FREE_SPACE_DEFAULT_16_ADJUSTABLE_0_TO_32' "$parent/docs/$PARENT_CONTRACT_NAME"
          grep -Fq 'localvol_shadow_sampling=SHADOW_INDEPENDENT' "$parent/docs/$PARENT_CONTRACT_NAME"
          sha256sum "$parent/renderer_sp_rend2_x64.dll" > .parent_renderer_sha256
          sha256sum "$parent/ioWolfSP.x64.exe" > .parent_exe_sha256
          sha256sum "$parent/Main/cgame_sp_x64.dll" > .parent_cgame_sha256
          sha256sum "$parent/Main/qagame_sp_x64.dll" > .parent_qagame_sha256
          sha256sum "$parent/Main/ui_sp_x64.dll" > .parent_ui_sha256
          sha256sum "$parent/renderer_sp_opengl1_x64.dll" > .parent_ogl1_sha256
          echo "Exact Run 35350987466 package/hash/manifest/contract PASS"

      - name: Reconstruct exact Run 35350987466 source postimage
        shell: bash
        run: |
          set -Eeuo pipefail
          parent="$(cat .parent_stage_path)"
          rm -rf source
          git clone -q "$UPSTREAM_REPO" source
          git -C source checkout -q --detach "$UPSTREAM_SHA"
          git -C source reset -q --hard "$UPSTREAM_SHA"
          git -C source clean -fdx
          cp "$parent/docs/$PARENT_CUMULATIVE_NAME" .parent-cumulative.patch
          git -C source apply --check --binary --whitespace=nowarn ../.parent-cumulative.patch
          git -C source apply --binary --whitespace=nowarn ../.parent-cumulative.patch
          git -C source apply --reverse --check --binary --whitespace=nowarn ../.parent-cumulative.patch
          git -C source add -N .
          git -C source diff --name-only "$UPSTREAM_SHA" -- | LC_ALL=C sort > .parent_actual_paths.txt
          python3 - <<'PY_PARENT'
          from pathlib import Path
          import re,subprocess
          lines=Path('.parent-cumulative.patch').read_text(encoding='utf-8').splitlines()
          records=[]; current=None
          for line in lines:
              m=re.match(r'diff --git a/(.+) b/(.+)$',line)
              if m:
                  if m.group(1)!=m.group(2): raise SystemExit(f'ERROR: rename/path mismatch: {line}')
                  current=m.group(2); continue
              if current and line.startswith('index '):
                  m=re.match(r'index ([0-9a-f]+)\.\.([0-9a-f]+)(?: [0-9]+)?$',line)
                  if not m: raise SystemExit(f'ERROR: malformed index for {current}: {line}')
                  records.append((current,m.group(2))); current=None
          if len(records)!=67 or len({p for p,_ in records})!=67: raise SystemExit(f'ERROR: cumulative section drift {len(records)}')
          expected=sorted(p for p,_ in records); actual=Path('.parent_actual_paths.txt').read_text().splitlines()
          if expected!=actual: raise SystemExit('ERROR: parent path set drift')
          for rel,prefix in records:
              got=subprocess.check_output(['git','-C','source','hash-object',rel],text=True).strip()
              if not got.startswith(prefix): raise SystemExit(f'ERROR: postimage blob drift {rel} expected={prefix} got={got}')
          print('Exact Run 35350987466 source postimage PASS: 67 files')
          PY_PARENT
          python3 - <<'PY_PARENT_FACTS'
          from pathlib import Path
          scene=Path('source/SP/code/rend2/tr_scene.c').read_text()
          main=Path('source/SP/code/rend2/tr_main.c').read_text()
          backend=Path('source/SP/code/rend2/tr_backend.c').read_text()
          init=Path('source/SP/code/rend2/tr_init.c').read_text()
          facts=[
            ('parent cap32','#define STATIC_PROMOTE_PRODUCTION_MAX_ACTIVE 32' in scene),
            ('parent maxlights32','r_staticPromoteMaxLights = ri.Cvar_Get( "r_staticPromoteMaxLights", "32", CVAR_ARCHIVE );' in init),
            ('parent range32','ri.Cvar_CheckRange( r_staticPromoteMaxLights, 1, 32, qtrue );' in init),
            ('parent allaccepted','r_staticPromoteAllAcceptedDelivery' in scene),
            ('parent delivered localvol','r_staticPromoteLocalVolAllActive' in backend),
            ('parent physical groups','R_DarkWolfStaticPromoteShadowGroupCanonicalId' in main),
            ('parent camera-following capture','VectorSubtract(fd->vieworg, shadowParms.or.origin, toView);' in main),
            ('parent capture default16','r_staticPromoteShadowCaptureOffset", "16"' in main),
            ('parent separate capture','dlightShadowCaptureOrigin' in main),
          ]
          bad=[n for n,ok in facts if not ok]
          if bad: raise SystemExit('ERROR: exact Run 35350987466 parent fact drift: '+', '.join(bad))
          print('Exact Run 35350987466 runtime/source facts PASS')
          PY_PARENT_FACTS
          git -C source add -A
          git -C source -c user.name='DarkWolf CI' -c user.email='darkwolf-ci@example.invalid' commit -q -m 'Reconstructed exact Run 35350987466 source postimage'
          parent_commit="$(git -C source rev-parse HEAD)"
          printf '%s\n' "$parent_commit" > .parent_source_commit
          test "$(git -C source rev-parse HEAD^)" = "$UPSTREAM_SHA"
          echo "Exact parent source reconstruction PASS: $parent_commit"

      - name: Materialize validate and apply Stable Synthetic Shadow Capture Final v2
        shell: bash
        run: |
          set -Eeuo pipefail
          cat > "$PATCHER_NAME.gz.b64" <<'PATCHER_B64'
          
          3lnM3z2IXMtpB7bkOAyqKsdBHfDWjrkx7zgVuz2rUTv/BaF4UEznLwAA
          PATCHER_B64
          base64 -d "$PATCHER_NAME.gz.b64" > "$PATCHER_NAME.gz"
          test "$(sha256sum "$PATCHER_NAME.gz" | awk '{print $1}')" = "$PATCHER_GZIP_SHA256" || { echo "ERROR: patcher gzip SHA drift" >&2; exit 1; }
          gzip -dc "$PATCHER_NAME.gz" > "$PATCHER_NAME"
          test "$(sha256sum "$PATCHER_NAME" | awk '{print $1}')" = "$PATCHER_SHA256" || { echo "ERROR: patcher SHA drift" >&2; exit 1; }
          python3 -m py_compile "$PATCHER_NAME"
          python3 "$PATCHER_NAME" --check source
          python3 "$PATCHER_NAME" source
          set +e
          python3 "$PATCHER_NAME" --check source >/tmp/repeat.out 2>&1
          repeat_rc=$?
          set -e
          test "$repeat_rc" -ne 0 || { echo "ERROR: patcher repeat guard failed" >&2; exit 1; }
          mapfile -t changed < <(git -C source diff --name-only "$(cat .parent_source_commit)" -- | LC_ALL=C sort)
          expected=(SP/code/rend2/tr_main.c)
          test "${#changed[@]}" -eq 1 || { printf 'ERROR: changed count=%s\n' "${#changed[@]}" >&2; printf '%s\n' "${changed[@]}" >&2; exit 1; }
          test "$(printf '%s\n' "${changed[@]}")" = "$(printf '%s\n' "${expected[@]}")" || { echo "ERROR: changed-file allowlist drift" >&2; printf '%s\n' "${changed[@]}" >&2; exit 1; }
          python3 - <<'PY_POLICY'
          from pathlib import Path
          import re,subprocess,hashlib
          main=Path('source/SP/code/rend2/tr_main.c').read_text()
          parent_commit=Path('.parent_source_commit').read_text().strip()
          parent=subprocess.check_output(['git','-C','source','show',f'{parent_commit}:SP/code/rend2/tr_main.c'],text=True)
          req=[
            ('marker','DARKWOLF_USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2' in main),
            ('stable cvar','r_staticPromoteShadowCaptureStable", "1"' in main),
            ('offset default32','r_staticPromoteShadowCaptureOffset", "32"' in main),
            ('cache max','USLRD_SYNTHETIC_CAPTURE_DIR_CACHE_MAX 1024' in main),
            ('map reset','R_DarkWolfStableSyntheticShadowCaptureResetForMap' in main),
            ('stable helper','R_DarkWolfStableSyntheticShadowCaptureDirection' in main),
            ('visibility seed','staticPromoteVisibilityTier > DWSP_TIER_SPHERE_PVS_CONNECTED' in main),
            ('cache diag','USLRD_CAPTURE_CACHE_INIT' in main),
            ('shadow diag','captureStable=%d captureDir=' in main),
          ]
          bad=[n for n,ok in req if not ok]
          if bad: raise SystemExit('ERROR: final-v2 mechanism missing: '+', '.join(bad))
          # Inspect the actual cubemap-application block, not the earlier helper/cvar marker.
          # fd->vieworg is intentionally present inside the helper for one-time cache seeding and stable=0 A/B.
          app_anchor='// Runtime Run 35350987466 proved that 32 units can de-embed the affected synthetic'
          a=main.index(app_anchor)
          b=main.index('shadowParms.zFar = physicalGroupRadius;',a)
          application=main[a:b]
          if 'VectorSubtract(fd->vieworg, shadowParms.or.origin' in application:
              raise SystemExit('ERROR: cubemap application still directly follows the view')
          if 'vec3_t toView' in application or 'VectorMA(shadowParms.or.origin, captureOffset, toView' in application:
              raise SystemExit('ERROR: legacy camera-following application path survived')
          if 'R_DarkWolfStableSyntheticShadowCaptureDirection(fd, &tr.refdef.dlights[bestLight]' not in application:
              raise SystemExit('ERROR: stable capture helper is not used by cubemap application')
          # A/B fallback is intentionally isolated inside the helper; stable production path caches once.
          hs=main.index('static qboolean R_DarkWolfStableSyntheticShadowCaptureDirection')
          he=main.index('static qboolean R_DarkWolfStaticPromoteShadowGroupEligible',hs)
          helper=main[hs:he]
          if helper.count('VectorSubtract(fd->vieworg, baseOrigin, outDirection);') != 2:
              raise SystemExit('ERROR: stable-helper view sampling contract drift')
          if 's_uslrdSyntheticCaptureDirValid[cacheIndex]' not in helper:
              raise SystemExit('ERROR: stable cache reuse missing')
          def block(s,sig):
              a=s.index(sig); b=s.index('{',a); d=0
              for i in range(b,len(s)):
                  if s[i]=='{': d+=1
                  elif s[i]=='}':
                      d-=1
                      if d==0:return s[a:i+1]
              raise SystemExit('unclosed '+sig)
          # No selector/budget primitive changes in the last attempt.
          for sig in ['static float R_DlightShadowSelectionScore','static qboolean R_DarkWolfShadowSourceIsPersistent','static float R_DarkWolfStaticPromoteShadowGroupSelectionScore']:
              if hashlib.sha256(block(parent,sig).encode()).digest()!=hashlib.sha256(block(main,sig).encode()).digest():
                  raise SystemExit('ERROR: protected shadow primitive changed: '+sig)
          diff=subprocess.check_output(['git','-C','source','diff','--unified=0',parent_commit,'--'],text=True)
          delta=[ln for ln in diff.splitlines() if (ln.startswith('+') or ln.startswith('-')) and not ln.startswith('+++') and not ln.startswith('---')]
          forbidden=['r_dlightShadowStrength','r_dlightShadowBias','r_dlightShadowSoftness','r_dlightShadowMapSize','r_dlightShadowMaxLights','r_staticPromoteMaxLights','r_staticPromoteGain','r_staticPromoteDirectGain','r_staticPromoteRadiusScale','r_staticPromoteMinRadius','r_staticPromoteMaxRadius','r_staticPromoteMaxColor','Cvar_Set(']
          for tok in forbidden:
              if any(tok in ln for ln in delta): raise SystemExit('ERROR: forbidden architecture/tuning changed: '+tok)
          added='\n'.join(ln[1:] for ln in delta if ln.startswith('+'))
          for tok in ['swf','escape1','factory','xlabs']:
              if re.search(r'(?i)(^|[^a-z0-9_])'+re.escape(tok)+r'([^a-z0-9_]|$)',added):
                  raise SystemExit('ERROR: map-specific source token introduced: '+tok)
          print('Stable Synthetic Shadow Capture Final v2 policy audit PASS')
          PY_POLICY
          echo "Final v2 source allowlist/policy PASS"

      - name: Generate final v2 delta cumulative and exact replay proofs
        shell: bash
        run: |
          set -Eeuo pipefail
          parent_commit="$(cat .parent_source_commit)"
          git -C source diff --binary --full-index "$parent_commit" -- SP/code/rend2/tr_main.c > "$DELTA_NAME"
          git -C source diff --binary --full-index "$UPSTREAM_SHA" -- > "$CUMULATIVE_NAME"
          test -s "$DELTA_NAME" && test -s "$CUMULATIVE_NAME"
          git -C source apply --reverse --check --binary --whitespace=nowarn "../$DELTA_NAME"
          rm -rf replay-delta replay-cumulative
          git clone -q source replay-delta
          git -C replay-delta checkout -q --detach "$parent_commit"
          git -C replay-delta apply --check --binary --whitespace=nowarn "../$DELTA_NAME"
          git -C replay-delta apply --binary --whitespace=nowarn "../$DELTA_NAME"
          cmp source/SP/code/rend2/tr_main.c replay-delta/SP/code/rend2/tr_main.c
          git clone -q source replay-cumulative
          git -C replay-cumulative checkout -q --detach "$UPSTREAM_SHA"
          git -C replay-cumulative apply --check --binary --whitespace=nowarn "../$CUMULATIVE_NAME"
          git -C replay-cumulative apply --binary --whitespace=nowarn "../$CUMULATIVE_NAME"
          mapfile -t all_paths < <(git -C source diff --name-only "$UPSTREAM_SHA" -- | LC_ALL=C sort)
          for rel in "${all_paths[@]}"; do cmp "source/$rel" "replay-cumulative/$rel"; done
          sha256sum "$DELTA_NAME" > .delta_sha256
          sha256sum "$CUMULATIVE_NAME" > .cumulative_sha256
          echo "Final v2 exact delta/reverse/replay + cumulative replay PASS"

      - name: Build canonical Windows x64 SP final v2 candidate
        shell: bash
        run: |
          set -Eeuo pipefail
          test -x source/SP/cross-make-mingw64.sh
          jobs="$(nproc)"; if [ "$jobs" -gt 4 ]; then jobs=4; fi
          (
            cd source/SP
            ./cross-make-mingw64.sh -j"$jobs" \
              BUILD_CLIENT=1 BUILD_SERVER=0 BUILD_GAME_SO=1 BUILD_GAME_QVM=0 \
              BUILD_BASEGAME=1 BUILD_RENDERER_REND2=1 USE_RENDERER_DLOPEN=1
          )
          mapfile -t renderers < <(find source/SP/build -type f -name renderer_sp_rend2_x64.dll -print | LC_ALL=C sort)
          mapfile -t cgames < <(find source/SP/build -type f -name cgame_sp_x64.dll -print | LC_ALL=C sort)
          mapfile -t exes < <(find source/SP/build -type f -name ioWolfSP.x64.exe -print | LC_ALL=C sort)
          mapfile -t ogl1s < <(find source/SP/build -type f -name renderer_sp_opengl1_x64.dll -print | LC_ALL=C sort)
          mapfile -t qagames < <(find source/SP/build -type f -name qagame_sp_x64.dll -print | LC_ALL=C sort)
          mapfile -t uis < <(find source/SP/build -type f -name ui_sp_x64.dll -print | LC_ALL=C sort)
          test "${#renderers[@]}" -eq 1 && test "${#cgames[@]}" -eq 1 && test "${#exes[@]}" -eq 1 && test "${#ogl1s[@]}" -eq 1 && test "${#qagames[@]}" -eq 1 && test "${#uis[@]}" -eq 1
          renderer="${renderers[0]}"; file "$renderer" | tee .renderer_file.txt; grep -Fq 'PE32+' .renderer_file.txt
          printf '%s\n' "$renderer" > .renderer_path
          sha256sum "$renderer" > .renderer_sha256
          echo "Canonical Windows x64 SP final v2 compile/output contract PASS"

      - name: Stage exact Run 35350987466 runtime and replace Rend2 renderer only
        shell: bash
        run: |
          set -Eeuo pipefail
          parent="$(cat .parent_stage_path)"; renderer="$(cat .renderer_path)"
          rm -rf runtime; mkdir -p runtime; stage="runtime/$RELEASE_BASENAME"
          cp -a "$parent" "$stage"; cp -f "$renderer" "$stage/renderer_sp_rend2_x64.dll"; rm -f "$stage/docs/SHA256SUMS.txt"
          cmp "$parent/ioWolfSP.x64.exe" "$stage/ioWolfSP.x64.exe"
          cmp "$parent/renderer_sp_opengl1_x64.dll" "$stage/renderer_sp_opengl1_x64.dll"
          cmp "$parent/Main/cgame_sp_x64.dll" "$stage/Main/cgame_sp_x64.dll"
          cmp "$parent/Main/qagame_sp_x64.dll" "$stage/Main/qagame_sp_x64.dll"
          cmp "$parent/Main/ui_sp_x64.dll" "$stage/Main/ui_sp_x64.dll"
          printf '%s\n' "$stage" > .stage_path
          echo "Runtime staging PASS: exact Run 35350987466 donor + new Rend2 renderer only"

      - name: Add final v2 controls provenance and one-pass runtime protocol
        shell: bash
        run: |
          set -Eeuo pipefail
          stage="$(cat .stage_path)"; mkdir -p "$stage/docs"
          cp "$PATCHER_NAME" "$stage/docs/$PATCHER_NAME"; cp "$DELTA_NAME" "$stage/docs/$DELTA_NAME"; cp "$CUMULATIVE_NAME" "$stage/docs/$CUMULATIVE_NAME"
          delta_sha="$(awk '{print $1}' .delta_sha256)"; cumulative_sha="$(awk '{print $1}' .cumulative_sha256)"; renderer_sha="$(awk '{print $1}' .renderer_sha256)"
          cat > "$stage/Main/USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2.cfg" <<'EOF_CFG'
          seta r_staticPromoteUnifiedResidency 1
          seta r_staticPromoteAllAcceptedDelivery 1
          seta r_staticPromoteMaxLights 32
          seta r_staticPromotePhysicalShadowGroup 1
          seta r_staticPromotePhysicalGroupMergeRadius 16
          seta r_staticPromotePhysicalGroupDiag 0
          seta r_staticPromoteLocalVolShadowContinuity 1
          seta r_staticPromoteLocalVolAllActive 1
          seta r_staticPromoteShadowCaptureStable 1
          seta r_staticPromoteShadowCaptureOffset 32
          seta r_dlightShadowMaxLights 3
          seta r_staticPromoteUnifiedDiag 0
          seta r_staticPromoteRootCauseDiag 0
          EOF_CFG
          cat > "$stage/Main/USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2_DIAG.cfg" <<'EOF_CFG'
          seta r_staticPromoteUnifiedResidency 1
          seta r_staticPromoteAllAcceptedDelivery 1
          seta r_staticPromoteMaxLights 32
          seta r_staticPromotePhysicalShadowGroup 1
          seta r_staticPromotePhysicalGroupMergeRadius 16
          seta r_staticPromotePhysicalGroupDiag 1
          seta r_staticPromoteLocalVolShadowContinuity 1
          seta r_staticPromoteLocalVolAllActive 1
          seta r_staticPromoteShadowCaptureStable 1
          seta r_staticPromoteShadowCaptureOffset 32
          seta r_dlightShadowMaxLights 3
          seta r_staticPromoteUnifiedDiag 1
          seta r_staticPromoteRootCauseDiag 0
          EOF_CFG
          cat > "$stage/Main/USLRD_FINAL_V1_DYNAMIC_CAPTURE_AB_V2.cfg" <<'EOF_CFG'
          seta r_staticPromoteUnifiedResidency 1
          seta r_staticPromoteAllAcceptedDelivery 1
          seta r_staticPromoteMaxLights 32
          seta r_staticPromotePhysicalShadowGroup 1
          seta r_staticPromoteLocalVolShadowContinuity 1
          seta r_staticPromoteLocalVolAllActive 1
          seta r_staticPromoteShadowCaptureStable 0
          seta r_staticPromoteShadowCaptureOffset 32
          seta r_dlightShadowMaxLights 3
          seta r_staticPromoteUnifiedDiag 1
          seta r_staticPromotePhysicalGroupDiag 1
          EOF_CFG
          cat > "$stage/Main/USLRD_CAPTURE0_AB_FINAL_V2.cfg" <<'EOF_CFG'
          seta r_staticPromoteUnifiedResidency 1
          seta r_staticPromoteAllAcceptedDelivery 1
          seta r_staticPromoteMaxLights 32
          seta r_staticPromotePhysicalShadowGroup 1
          seta r_staticPromoteLocalVolShadowContinuity 1
          seta r_staticPromoteLocalVolAllActive 1
          seta r_staticPromoteShadowCaptureStable 1
          seta r_staticPromoteShadowCaptureOffset 0
          seta r_dlightShadowMaxLights 3
          seta r_staticPromoteUnifiedDiag 1
          seta r_staticPromotePhysicalGroupDiag 1
          EOF_CFG
          cat > "$stage/RUN_USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2.cmd" <<'EOF_CMD'
          @echo off
          start "" ioWolfSP.x64.exe +set cl_renderer rend2 +exec USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2.cfg
          EOF_CMD
          cat > "$stage/RUN_USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2_DIAG.cmd" <<'EOF_CMD'
          @echo off
          start "" ioWolfSP.x64.exe +set cl_renderer rend2 +exec USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2_DIAG.cfg
          EOF_CMD
          cat > "$stage/docs/$CONTRACT_NAME" <<EOF_CONTRACT
          DARKWOLF / IORTCW REND2 SP - USLRD STABLE SYNTHETIC SHADOW CAPTURE FINAL V2
          parent_run=$PARENT_RUN_ID
          parent_head=$PARENT_RUN_HEAD_SHA
          parent_artifact_id=$PARENT_ARTIFACT_ID
          parent_artifact_digest=$PARENT_ARTIFACT_DIGEST
          parent_inner_sha256=$PARENT_INNER_SHA256
          upstream_sha=$UPSTREAM_SHA
          status=FINAL_FIX_CANDIDATE_RUNTIME_ACCEPTANCE_REQUIRED
          attempt_budget=FINAL_ATTEMPT_2_OF_2
          source_delta_files=1
          source_delta_allowlist=SP/code/rend2/tr_main.c
          patcher_sha256=$PATCHER_SHA256
          delta_sha256=$delta_sha
          cumulative_sha256=$cumulative_sha
          renderer_sha256=$renderer_sha
          runtime_binary_replacements=renderer_sp_rend2_x64.dll_ONLY
          runtime_evidence_A=RUN_35350987466_ALL_693_STATUS_SNAPSHOTS_ACTIVE15_MAXLIGHTS32_OVERFLOW0_SHADOWBUDGET3
          runtime_evidence_B=OFFSET32_USER_REPORT_THREE_LAMP_BRIGHTNESS_STABLE
          runtime_evidence_C=OFFSET32_CAPTURE_ORIGIN_MOVES_WITH_VIEW_AND_VIDEO_SHOWS_GIANT_CAMERA_COUPLED_PLAYER_SILHOUETTE
          root_cause=FINAL_V1_DEEMBED_DIRECTION_RECOMPUTED_FROM_FD_VIEWORG_EVERY_FRAME_SO_SHADOW_CUBEMAP_CAMERA_FOLLOWS_PLAYER
          direct_delivery=UNCHANGED_PARENT_ALL_ACCEPTED_UP_TO_32
          staticpromote_max_active=UNCHANGED_32
          staticpromote_maxlights_default=UNCHANGED_32
          persistent_shadow_budget_default=UNCHANGED_3
          persistent_shadow_budget_runtime_cvar=r_dlightShadowMaxLights_USER_ADJUSTABLE
          shadow_selector=UNCHANGED_PARENT_DISTANCE_RADIUS_LUMINANCE
          shadow_capture_offset_default=32
          shadow_capture_offset_range=0_TO_32
          shadow_capture_direction=PER_SYNTHETIC_FIXTURE_WORLD_SPACE_CACHE
          shadow_capture_direction_seed=FIRST_SURFACE_OR_SPHERE_PVS_CONNECTED_VIEW_ONLY
          shadow_capture_direction_lifetime=MAP_SCOPED_REUSED_ACROSS_SHADOW_SLOT_TRANSFERS
          shadow_capture_dynamic_ab=r_staticPromoteShadowCaptureStable_0
          shadow_capture_direct_origin=UNCHANGED
          player_shadow_caster=UNCHANGED_AND_RETAINED
          localvol=UNCHANGED_PARENT_ALLACTIVE_SHADOW_INDEPENDENT
          shadow_strength_resolution_bias_softness=UNCHANGED
          map_special_case=NONE
          bloom_tonemap_exposure_change=NONE
          firevol_change=NONE
          ssgi_sunvol_change=NONE
          mtr_material_change=NONE
          gameplay_ai_map_content_change=NONE
          EOF_CONTRACT
          cat > "$stage/docs/$TEST_PLAN_NAME" <<'EOF_TEST'
          USLRD STABLE SYNTHETIC SHADOW CAPTURE FINAL V2 - FINAL ATTEMPT 2/2

          PRIMARY SWF TEST
          1. Start RUN_USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2_DIAG.cmd.
          2. Keep the same deduplicated SWF synthetic file used for Run 35350987466.
          3. Do not alter Bloom/ToneMap/Exposure/material gain/radius/shadow strength.
          4. Walk the same three-lamp row (fixtures 1000016 / 1000009 / 1000011) with the same approach/retreat/turn route.

          DIRECT-LIGHT INVARIANT ALREADY PROVEN IN PARENT AND MUST REMAIN TRUE
          - acceptedFixtures=15 -> submittedPromoted=15 -> active=15.
          - maxLights=32, overflow=0.
          - PVS tier may move, but accepted Direct lights never switch off.

          FINAL V2 CAPTURE INVARIANT
          - r_staticPromoteShadowCaptureStable=1 and r_staticPromoteShadowCaptureOffset=32 by default.
          - USLRD_CAPTURE_CACHE_INIT should appear once when each synthetic fixture first receives a valid visible seed direction.
          - For the same fixture, later USLRD_SHADOW_GROUP lines must keep the same captureDir even while the player moves.
          - The capture origin may change only if the physical group base origin itself changes; for singleton fixtures 1000016/1000009/1000011 it should remain fixed while that fixture owns a slot.
          - The huge head/torso silhouette that follows the camera across wall/ceiling must disappear.
          - A real player shadow may move/deform as the player moves, but its projection must be anchored to the fixed lamp, not to the camera.

          SHADOW BUDGET
          - Default remains r_dlightShadowMaxLights=3.
          - The user may set 4 or 5 at runtime without rebuilding; Direct active count remains independent.

          OPTIONAL A/B ONLY IF NEEDED
          - exec USLRD_FINAL_V1_DYNAMIC_CAPTURE_AB_V2.cfg reproduces camera-following offset32 behavior (stable=0) as the control.
          - exec USLRD_CAPTURE0_AB_FINAL_V2.cfg keeps stable mode but disables de-embedding.
          - No additional YML is intended after this final candidate.

          REGRESSION ACCEPTANCE
          - SWF three-lamp row stable brightness and coherent player/world shadows.
          - Factory, Escape1 doctor office and X-Labs unchanged or improved.
          - Weapon/Tesla/explosion transient shadows unaffected.
          - Startup, map transition, save/load and FPS checked; no crash.
          EOF_TEST
          cat > "$stage/$README_NAME" <<'EOF_README'
          DARKWOLF / ioRTCW Rend2 SP - USLRD Stable Synthetic Shadow Capture Final v2

          Run 35350987466 proved the Direct architecture: all 15 accepted SWF synthetic fixtures remained active in every recorded status sample with maxLights=32, overflow=0 and a separate persistent shadow budget of 3. At offset 32 the user also observed that the three-lamp row stopped visible bright/dim switching.

          The remaining artifact was created by final-v1 itself. The cubemap de-embed direction was recomputed every frame from fd->vieworg. Runtime logs show each lamp's Direct origin stayed fixed while capture origin moved tens of units with the player, and video shows a large player-like silhouette sliding over the wall/ceiling. The player model is supposed to cast into point-shadow maps; the bug is the moving light camera, not the caster.

          Final v2 keeps the proven offset magnitude 32 but learns one world-space de-embed direction per Synthetic fixture only after that fixture is genuinely PVS-connected, caches that direction for the map, and reuses it across movement and shadow-slot transfers. Direct origin/radius/color, selector, 3-slot budget, LocalVol and all unrelated rendering systems are unchanged.

          Diagnostic launcher: RUN_USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2_DIAG.cmd
          Production-style launcher: RUN_USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2.cmd
          EOF_README
          cp "$stage/$README_NAME" "$stage/docs/$README_NAME"
          cat > "$stage/docs/$VALIDATION_NAME" <<EOF_VALIDATION
          USLRD STABLE SYNTHETIC SHADOW CAPTURE FINAL V2 - CI VALIDATION RECORD
          parent_run=$PARENT_RUN_ID
          parent_head=$PARENT_RUN_HEAD_SHA
          parent_artifact_digest=$PARENT_ARTIFACT_DIGEST
          parent_inner_sha256=$PARENT_INNER_SHA256
          upstream_sha=$UPSTREAM_SHA
          parent_package_manifest=PASS
          parent_source_reconstruction=PASS_67_FILES
          patcher_identity_pycompile=PASS
          patcher_check_apply_repeat_guard=PASS
          changed_file_allowlist=PASS_1_FILE_TR_MAIN_ONLY
          no_map_special_case=PASS
          protected_shadow_selector_primitives=UNCHANGED
          direct_delivery_architecture=UNCHANGED_PARENT_32
          localvol_architecture=UNCHANGED_PARENT
          forbidden_lighting_tuning=UNCHANGED
          delta_reverse_check=PASS
          delta_clean_replay=PASS
          cumulative_clean_replay=PASS
          mingw_sp_compile=PASS_THIS_RUN
          pe32_renderer=PASS
          runtime_status=FINAL_FIX_CANDIDATE_ACCEPTANCE_REQUIRED
          EOF_VALIDATION
          echo "Final v2 controls/provenance/runtime protocol PASS"

      - name: Final v2 package policy audit and SHA manifest
        shell: bash
        run: |
          set -Eeuo pipefail
          stage="$(cat .stage_path)"
          required=(
            ioWolfSP.x64.exe renderer_sp_rend2_x64.dll renderer_sp_opengl1_x64.dll
            Main/cgame_sp_x64.dll Main/qagame_sp_x64.dll Main/ui_sp_x64.dll OpenAL64.dll SDL264.dll
            Main/autoexec.cfg RUN_DARKWOLF_PRODUCTION.cmd
            Main/USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2.cfg
            Main/USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2_DIAG.cfg
            Main/USLRD_FINAL_V1_DYNAMIC_CAPTURE_AB_V2.cfg
            Main/USLRD_CAPTURE0_AB_FINAL_V2.cfg
            RUN_USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2.cmd
            RUN_USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2_DIAG.cmd
            "$README_NAME" "docs/$README_NAME" "docs/$CONTRACT_NAME" "docs/$VALIDATION_NAME"
            "docs/$TEST_PLAN_NAME" "docs/$PATCHER_NAME" "docs/$DELTA_NAME" "docs/$CUMULATIVE_NAME"
          )
          for rel in "${required[@]}"; do test -s "$stage/$rel" || { echo "ERROR: required package file missing: $rel" >&2; exit 1; }; done
          grep -Fq 'r_staticPromoteAllAcceptedDelivery 1' "$stage/Main/USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2.cfg"
          grep -Fq 'r_staticPromoteMaxLights 32' "$stage/Main/USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2.cfg"
          grep -Fq 'r_staticPromoteShadowCaptureStable 1' "$stage/Main/USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2.cfg"
          grep -Fq 'r_staticPromoteShadowCaptureOffset 32' "$stage/Main/USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2.cfg"
          grep -Fq 'r_dlightShadowMaxLights 3' "$stage/Main/USLRD_STABLE_SYNTHETIC_SHADOW_CAPTURE_FINAL_V2.cfg"
          grep -Fq 'r_staticPromoteShadowCaptureStable 0' "$stage/Main/USLRD_FINAL_V1_DYNAMIC_CAPTURE_AB_V2.cfg"
          grep -Fq 'r_staticPromoteShadowCaptureOffset 0' "$stage/Main/USLRD_CAPTURE0_AB_FINAL_V2.cfg"
          grep -Fq 'cl_renderer rend2' "$stage/RUN_DARKWOLF_PRODUCTION.cmd"
          parent="$(cat .parent_stage_path)"
          cmp "$parent/ioWolfSP.x64.exe" "$stage/ioWolfSP.x64.exe"
          cmp "$parent/renderer_sp_opengl1_x64.dll" "$stage/renderer_sp_opengl1_x64.dll"
          cmp "$parent/Main/cgame_sp_x64.dll" "$stage/Main/cgame_sp_x64.dll"
          cmp "$parent/Main/qagame_sp_x64.dll" "$stage/Main/qagame_sp_x64.dll"
          cmp "$parent/Main/ui_sp_x64.dll" "$stage/Main/ui_sp_x64.dll"
          if find "$stage" -type f -iname 'pak*.pk3' -print -quit | grep -q .; then echo "ERROR: commercial pak*.pk3 found" >&2; exit 1; fi
          if find "$stage" -type f \( -iname '*.sav' -o -iname 'wolfconfig.cfg' \) -print -quit | grep -q .; then echo "ERROR: user save/config found" >&2; exit 1; fi
          rm -f "$stage/docs/SHA256SUMS.txt"
          (cd "$stage" && find . -type f ! -path './docs/SHA256SUMS.txt' -print0 | LC_ALL=C sort -z | xargs -0 sha256sum > docs/SHA256SUMS.txt && sha256sum -c docs/SHA256SUMS.txt)
          echo "Final v2 package/runtime/commercial-asset policy PASS"

      - name: Package full SP USLRD final v2 candidate
        shell: bash
        run: |
          set -Eeuo pipefail
          stage="$(cat .stage_path)"
          rm -rf out; mkdir -p out
          archive="out/${RELEASE_BASENAME}.zip"
          (cd runtime && find "$RELEASE_BASENAME" -type f -print | LC_ALL=C sort | zip -X -9 "$GITHUB_WORKSPACE/$archive" -@)
          test -s "$archive"; unzip -tq "$archive"
          sha256sum "$archive" | sed "s#  out/#  #" > "out/${RELEASE_BASENAME}.zip.sha256"
          cat "out/${RELEASE_BASENAME}.zip.sha256"

      - name: Upload full SP USLRD final v2 artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.RELEASE_BASENAME }}
          path: |
            out/${{ env.RELEASE_BASENAME }}.zip
            out/${{ env.RELEASE_BASENAME }}.zip.sha256
          if-no-files-found: error
          compression-level: 0
