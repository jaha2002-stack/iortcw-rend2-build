# Следующий шаг после этого kit

Если DLIGHTDBG показывает факелы/огни:
  делаем Kit B: cubemap depth pass.

Если DLIGHTDBG показывает только muzzle/explosion, но не факелы:
  нужно искать, создают ли факелы dlight в game/cgame логике.

Если DLIGHTDBG ничего не показывает:
  r_dlightMode 2/3 path не получает dlights в этой сцене, надо искать выше по renderer pipeline.
